Use <code>gma_noise.py</code> for updating noise along with beta in the optimization

Use <code>gma_wishart.py</code> for a temporal kernel defined by the wishart distribution instead of using identity
